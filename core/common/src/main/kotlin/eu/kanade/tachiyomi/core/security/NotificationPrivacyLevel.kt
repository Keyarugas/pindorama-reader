package eu.kanade.tachiyomi.core.security

/** NORMAL deliberately preserves existing notification and legacy hiding preferences. */
enum class NotificationPrivacyLevel {
    NORMAL,
    DISCREET,
    PRIVATE,
}
